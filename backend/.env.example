# 鏈嶅姟鍣ㄩ厤缃?NODE_ENV=development
PORT=3000
# Backend .env example
# 使用方法：复制此文件为 backend/.env 并根据环境填写实际值

# ============================================
# 运行模式与端口（选择一项启用）
# ============================================
# 切换方法：把需要启用的那一块前面的 `#` 去掉，并把另一块注释掉。

# ---------- 生产 / 服务器（示例 - 注释掉以禁用） ----------
#NODE_ENV=production
#PORT=3000
#BASE_URL=https://your.production.domain
#ALLOWED_ORIGINS=https://your.production.domain

# ---------- 本地 / 开发（默认启用） ----------
NODE_ENV=development
PORT=3000
BASE_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================
# MySQL 数据库配置
# ============================================
# 在 Docker 环境中 DB_HOST 可能是 mysql，开发时通常为 localhost
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_database_password
DB_NAME=campus_trade

# ============================================
# JWT 配置（生产环境必须修改为强密钥，至少32个字符）
# ============================================
JWT_SECRET=your_jwt_secret_key_min_32_characters

# ============================================
# 邮件服务配置（用于验证码等功能）
# ============================================
# 示例：QQ 邮箱（使用授权码）
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=your_email@qq.com
SMTP_PASS=your_smtp_authorization_code

# 前端地址（用于 CORS 或在邮件中创建链接）
FRONTEND_URL=http://localhost:5173
