# Docker 部署环境变量配置示例
# 使用方法：复制此文件为 .env 并修改相应的值

# ============================================
# 应用端口配置
# ============================================
APP_PORT=80

# ============================================
# MySQL 数据库配置
# ============================================
MYSQL_ROOT_PASSWORD=your_root_password_change_in_production
MYSQL_PORT=3306
DB_HOST=mysql
DB_PORT=3306
DB_USER=campus_user
DB_PASSWORD=your_secure_password_here
DB_NAME=campus_trade

# ============================================
# JWT 配置（生产环境必须修改为强密钥，至少32个字符）
# ============================================
JWT_SECRET=your_jwt_secret_key_min_32_characters_change_in_production_environment

# ============================================
# 邮件服务配置（必需，用于验证码功能）
# ============================================
# ⚠️ 重要：必须配置此部分，否则验证码功能无法使用！
# 
# QQ 邮箱（推荐）：
#   1. 登录 QQ 邮箱网页版
#   2. 设置 → 账户 → 开启 "IMAP/SMTP服务"
#   3. 生成授权码（不是QQ密码！）
#   4. 将授权码填入 SMTP_PASS
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=your_email@qq.com
SMTP_PASS=your_smtp_authorization_code

# 163 邮箱示例：
# SMTP_HOST=smtp.163.com
# SMTP_PORT=465
# SMTP_USER=your_email@163.com
# SMTP_PASS=your_smtp_authorization_code

# Gmail 示例（需要开启"允许不够安全的应用"）：
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# ============================================
# 服务器配置
# ============================================
## ==========================
# 运行模式与端口（选择一项启用）
## ==========================
# 说明：下面提供两套配置块 —— "生产/服务器"（コメント掉以禁用）和 "本地/开发"（默认启用）。
# 切换方法：把需要启用的那一块前面的注释（#）去掉，并把另一块加上注释。

# ---------- 生产 / 服务器（示例 - 注释掉以禁用） ----------
#NODE_ENV=production
#PORT=3000
#BASE_URL=https://your.production.domain
#ALLOWED_ORIGINS=https://your.production.domain,https://www.your.domain

# ---------- 本地 / 开发（默认启用） ----------
NODE_ENV=development
PORT=3000
BASE_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

## ==========================
# 快速切换说明：
# - 要启用生产配置，请把生产块的行前的 `#` 删除，并给本地块每行添加 `#`。
# - 要回到本地开发，反向操作即可。

